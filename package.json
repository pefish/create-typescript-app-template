{
  "name": "@pefish/app-name",
  "version": "0.0.1",
  "description": "app-description",
  "author": "pefish <pefish@qq.com>",
  "dependencies": {
    "@pefish/js-logger": "^1.0.4",
    "@pefish/js-starter": "^1.0.0",
    "dotenv": "^16.4.5"
  },
  "appName": "app-name",
  "bin": {
    "app-name": "app.js"
  },
  "scripts": {
    "build": "rm -rf lib && tsc",
    "start": "LOG_LEVEL=debug ts-node src/start.ts",
    "test": "mocha --timeout 20000 --exit --require ts-node/register src/**/*.test.ts",
    "t": "mocha --timeout 20000 --exit --require ts-node/register",
    "docker": "docker build -t app-name .",
    "prepare": "npm run test && npm run build",
    "prepublish": "check-node-version --npm \">=4\" || npm run prepare"
  },
  "devDependencies": {
    "@types/chai": "^4.3.12",
    "@types/mocha": "^5.2.7",
    "@types/node": "^14.18.63",
    "check-node-version": "^1.1.2",
    "mocha": "^10.3.0",
    "ts-node": "^10.9.1",
    "typescript": "^5.0.4"
  }
}
